[pytest]
python_files = Test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src

addopts =
    -ra
    --strict-markers

markers =
    sanity: quick smoke checks
    regression: full or broad regression coverage
    logic: business-logic focused tests
